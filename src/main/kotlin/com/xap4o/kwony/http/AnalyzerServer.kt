package com.xap4o.kwony.http

import endWithJson
import io.vertx.core.http.HttpMethod
import io.vertx.ext.web.Router

object AnalyzerServer {
//    val api = Router.router(vertx).apply {
//        route(HttpMethod.POST, "/analyze").handler { context ->
//            context.request().bodyHandler { buffer -> Unit }
//            context.response().endWithJson(listOf(1, 2, 3))
//        }
//    }

//            path("analyze") {
//                post {
//                    entity(as[Tweet]) { tweet =>
//                    complete(HttpEntity(ContentTypes.`application/json`, (tweet.text.length % 2 == 0).toString))
//                }
//                }
//            }
}


